import { useState } from "react"

export default function Index() {

    const [config, setConfig] = useState({
        brand: 'INTEL',
        budget: 0,
        use: 'gaming',
        format: 'atx',
        design: 'minimal',
        refrigeration: 'stock'
    });

    const newConfig = () => {
        
    }

    return (
        <div>
            
        </div>
    )
}
